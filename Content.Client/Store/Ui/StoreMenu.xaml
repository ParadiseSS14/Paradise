<controls:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
    xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
    xmlns:userInterfaces="clr-namespace:Content.Client._DV.UserInterfaces"
    xmlns:xe="clr-namespace:Content.Client.UserInterface.XamlExtensions"
    Title="{Loc 'store-ui-default-title'}"
    MinSize="512 512"
    SetSize="750 700">
    <BoxContainer Orientation="Vertical">
        <BoxContainer Orientation="Vertical" VerticalExpand="True" Margin="8">
            <BoxContainer Orientation="Horizontal" VerticalExpand="True">
                <BoxContainer Orientation="Vertical" VerticalExpand="True" MinSize="160 128">
                    <BoxContainer Margin="4,4,4,4" Orientation="Horizontal">
                        <PanelContainer StyleClasses="button" HorizontalExpand="True" Margin="0 0 8 0">
                            <RichTextLabel
                                Name="BalanceInfo"
                                Access="Public"/>
                        </PanelContainer>
                        <userInterfaces:IconButton
                            Name="WithdrawButton"
                            HorizontalAlignment="Right"
                            Icon="{xe:Tex '/Textures/Interface/eject.png'}"/>
                        <Button
                            Name="RefundButton"
                            MinWidth="64"
                            HorizontalAlignment="Right"
                            Text="{Loc 'store-ui-refund-text'}" />
                    </BoxContainer>
                    <LineEdit Name="SearchBar" Margin="4" PlaceHolder="Search" HorizontalExpand="True" />
                    <PanelContainer VerticalExpand="True" StyleClasses="SurfacePrimary">
                        <BoxContainer Name="CategoryListContainer" Orientation="Vertical" Margin="8 8 0 8" MinWidth="120" SeparationOverride="4">
                            <!--  Category buttons are added here by code  -->
                        </BoxContainer>
                    </PanelContainer>
                </BoxContainer>
                <Control MinWidth="6"/>
                <PanelContainer VerticalExpand="True" HorizontalExpand="True" MinSize="100 256" StyleClasses="SurfacePrimary">
                    <ScrollContainer
                        Name="StoreListingsScroll"
                        HScrollEnabled="False"
                        HorizontalExpand="True"
                        MinSize="100 256"
                        SizeFlagsStretchRatio="2"
                        VerticalExpand="True"
                        Margin="4">
                        <BoxContainer
                            Name="StoreListingsContainer"
                            MinSize="100 256"
                            Orientation="Vertical"
                            SizeFlagsStretchRatio="2"
                            VerticalExpand="True"
                            SeparationOverride="8">
                            <!--  Listings are added here by code  -->
                        </BoxContainer>
                    </ScrollContainer>
                </PanelContainer>
            </BoxContainer>
        </BoxContainer>

        <!-- Footer -->
        <BoxContainer Orientation="Vertical" Name="TraitorFooter">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'store-ui-traitor-flavor'}" StyleClasses="WindowFooterText" />
                <Label Text="{Loc 'store-ui-traitor-warning'}" StyleClasses="WindowFooterText"
                       HorizontalAlignment="Right" HorizontalExpand="True" Margin="0 0 5 0" />
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
